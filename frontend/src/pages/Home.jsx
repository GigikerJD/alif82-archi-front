import { ButtonGroup, Button, Container, Link, Typography, Box } from "@mui/material"
import { AppAuth } from "../config/AppProvider"


export const Home = () => {

    return(
        <>
            <Container maxWidth="lg" sx={{ border: '1px solid lightblue'}}>
                
            </Container>
        </>
    )
}